"""User-facing dataset download / preparation scripts.

Thin wrappers around :mod:`data_platform.storage` and :mod:`data_platform.datasets`
that make experiment-time use ergonomic (single CLI, MLflow logging
baked in, common Alpaca schema enforced).

Run any script with ``--help`` for usage.
"""
